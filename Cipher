
import string

print('How many letters will you enter?')
num_of_letters = int(input())

letter_values = dict()
for index, letter in enumerate(string.ascii_lowercase):
    letter_values[letter] = index + 1

#print(list(letter_values.items()))

for x in range(0,num_of_letters):
    print("Input a letter")
    first_letter = input()
    print(letter_values.get(first_letter))
    print(letter_values[first_letter]*2)

#print(values.items())

#for first_letter, number in letter_values.items():
#if first_letter == 'a':
