print("write a sentence") 

new_sentence = input()

print("Length of sentence is " + str(len(new_sentence)))

print(new_sentence[2])
squares = []


import string
#this is a simple  into a dictionary
new_sentence.split()



#This assigns values to numbers
values = dict()
for index, letter in enumerate(string.ascii_lowercase):
   values[letter] = index + 1
print(values)


for letter, number in values.items():
  a1 = number*2
  print(letter + ' numnber is ' + str(number))


leng = len(new_sentence)
for x in new_sentence:
  squares.append(x)
  #if x = a or b or c or d or e;
  
  
    
    
  
  
print(squares)

#leng = len(new_sentence)
##for x in (0,leng);
#  print(x)
