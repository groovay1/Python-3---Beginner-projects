word = input('Input a word')
letter = input('Input a letter in the word and I will count how many times it comes up')
def countLetters(word,letter):
    count = 0
    for character in word:
        if character == letter:
            count = count + 1
    return count
print ('your letter was encountered',countLetters(word,letter),'times')


